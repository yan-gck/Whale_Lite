<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dsh.listeningplayer"
    android:versionCode="4"
    android:versionName="1.3">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34" />

    <!-- 离线播放不需要网络权限；保留 INTERNET 是为了方便以后接入在线资源 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 后台/锁屏保活：
         前台服务（带常驻通知）让播放不被系统回收，锁屏后也能继续听；
         WAKE_LOCK 让熄屏时 CPU 保持运行，否则音频会断续；
         POST_NOTIFICATIONS 是 Android 13+ 显示那个通知所必需（运行时申请）。
         注意：这些都不是存储权限，补充包依旧不需要任何权限。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 播放前台服务：常驻通知 + 媒体会话（锁屏/通知栏可控制播放） -->
        <service
            android:name=".PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>
</manifest>
